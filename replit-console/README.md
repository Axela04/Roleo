# Replit Audit & Upload Console

Set globals before serving:
```html
<script>
window.ROLEO_API_URL = 'https://api.roleo.app';
window.ROLEO_ADMIN_TOKEN = '...';
</script>
```

Then serve static file and restrict access internally.
